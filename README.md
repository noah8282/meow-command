# Meow Command
A custom Linux command written by Mohit that displays A cat 🐱

## Installation

To install `meow` directly from your terminal, run this single command:

```bash
sudo curl -sL https://raw.githubusercontent.com/noah8282/meow-command/refs/heads/main/meow -o /usr/local/bin/meow && sudo chmod +x /usr/local/bin/meow
